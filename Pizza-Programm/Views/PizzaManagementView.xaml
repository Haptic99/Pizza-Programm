<UserControl x:Class="Pizza_Programm.Views.PizzaManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pizza_Programm.Views"
             xmlns:vm="clr-namespace:Pizza_Programm.ViewModels"
             xmlns:converters="clr-namespace:Pizza_Programm.Converters"
             mc:Ignorable:d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <vm:PizzaManagementViewModel/>
    </UserControl.DataContext>

    <UserControl.Loaded>
        <Binding Path="LoadDataCommand" Mode="OneWay"/>
    </UserControl.Loaded>

    <UserControl.Resources>
        <converters:NullToBooleanConverter x:Key="NullToBooleanConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <ColumnDefinition Width="*"/>
    <ColumnDefinition Width="200"/>
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Padding="5">
        <StackPanel>
            <TextBlock Text="Neue Pizza erstellen" FontWeight="Bold"/>
            <Label Content="Name:"/>
            <TextBox Text="{Binding NewPizzaName, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Preis:"/>
            <TextBox Text="{Binding NewPizzaPrice, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Pizza hinzufügen" Command="{Binding AddPizzaCommand}" Margin="0,10"/>

            <Separator Margin="0,10"/>

            <TextBlock Text="Pizzen verwalten" FontWeight="Bold"/>
            <ListView ItemsSource="{Binding Pizzas}" 
                          SelectedItem="{Binding SelectedPizza}"
                          Height="250">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Preis" DisplayMemberBinding="{Binding Price, StringFormat=C}" Width="70"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Content="Auswahl löschen" Command="{Binding DeletePizzaCommand}" Background="Tomato" Margin="0,5"/>
        </StackPanel>
    </Border>

    <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="5" Margin="10,0,10,0">
        <StackPanel>
            <TextBlock Text="Zutaten für:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedPizza.Name, FallbackValue='(Keine Pizza ausgewählt)'}" 
                           FontSize="16" Foreground="DarkBlue" Margin="0,5,0,10"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="350">
                <ItemsControl ItemsSource="{Binding IngredientSelections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Ingredient.Name}" 
                                          IsChecked="{Binding IsSelected}" 
                                          Margin="5"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Button Content="Zutaten speichern" 
                        Command="{Binding SavePizzaIngredientsCommand}" 
                        IsEnabled="{Binding SelectedPizza, Converter={StaticResource NullToBooleanConverter}}" 
                        Margin="0,10,0,0"
                        Background="LightGreen"/>
        </StackPanel>
    </Border>

    <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" Padding="5">
        <StackPanel>
            <TextBlock Text="Preisdetails (Beispiel)" FontWeight="Bold"/>
            <TextBlock Text="Hier könnte eine Preiskalkulation basierend auf den Zutaten stehen." TextWrapping="Wrap"/>
        </StackPanel>
    </Border>

    </Grid>
</UserControl>